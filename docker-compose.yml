version: '3.8'

services:
  app:
    image: node:18
    working_dir: /usr/src/app
    volumes:
      - .:/usr/src/app
    ports:
      - "4000:4000"
    command: ["npm", "start"]
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
